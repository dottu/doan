extends ../views/layout/nav.pug

block content
  section.content
    .card.card-solid
      .card-body.pb-0
        .row.d-flex.align-items-stretch
          if(book && book.length)
            each bk in book
              .col-12.col-sm-6.col-md-4.d-flex.align-items-stretch
                .card.bg-light
                  a(href='/thong-tin-book/'+bk.id) 
                    img(src="/uploads/"+bk.image)
                  br
                  .card-body.pt-0#title
                    span #{bk.title}
                  .card-body.pt-0#title
                    span 
                      b #{bk.cost}
                  //- .text-right
                  //-     a.btn.btn-sm.bg-teal(href='#')
                  //-         i.fas.fa-comments Mua hang
                  //-     a.btn.btn-sm.btn-primary(href='#')
                  //-         i.fas.fa-user
                  //-         |  Them gio hang
                  //- .card-footer
                  //-   nav(aria-label='Contacts Page Navigation')
                  //-     ul.pagination.justify-content-center.m-0

                  //-       - for (var i =1 ; i <= page ; i++)
                  //-           li.page-item.active
                  //-             a.page-link(href='/search?q='+b)
                  //-               span #{i}